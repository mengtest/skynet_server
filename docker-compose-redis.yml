version: '3.8'
services:
  redis:
    image: redis:alpine 
    container_name: 'redis' 
    restart: always
    environment: 
      TZ: Asia/Shanghai
    volumes: 
      - ./redisdata:/data
      - ./tools/redis.conf:/usr/local/etc/redis/redis.conf
    ports: 
      - "6379:6379" 
  