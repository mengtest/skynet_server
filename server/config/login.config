include "config.path"

thread = 8
harbor = 0
start = "main"
nodename = "login"

luaservice = luaservice.."server-"..nodename.."/?.lua"

if $DAEMON then
	logger = root.."logs/"..nodename
	daemon = root.."pids/"..nodename..".pid"
end
