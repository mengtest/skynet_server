include "config.path"

thread = 8
harbor = 0
start = "main"
nodename = "game1"

luaservice = luaservice.."server-game".."/?.lua"

if $DAEMON then
	logger = root.."logs/"..nodename
	daemon = root.."pids/"..nodename..".pid"
end
